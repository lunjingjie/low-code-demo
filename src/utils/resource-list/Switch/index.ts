import { IMaterial } from '/#/common';
import { Switch } from 'ant-design-vue';

export const SwitchResource: IMaterial = {
  name: '开关按钮',
  component: Switch,
  resource: {
    icon: '',
    color: 'yellow',
  }
};
